* Performance fixes, thanks to kotmatross
  * Replace GL11.glBindTexture(GL11.GL_TEXTURE_2D, previousTexture); with mc.getTextureManager().bindTexture(TextureMap.locationBlocksTexture); in FootprintManager
  * Removed OpenGlHelper.setActiveTexture and GL11.glGetInteger in FootprintManager
* Added Dragon Block C compat when aiming in Decoupled Camera mode
* Fixed computeAimRotation raycasting blocks behind the camera
